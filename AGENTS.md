# Agent notes

This is a dawn Eastern Range **blockhouse**, not a product dashboard.

## Do not reverse

- Analog desk (formica / brass / mosaic / HOLD clock).
- `file://` double-click. No bundler on the play path.
- Physics lives in `physics.js`. Tests first. Do not cut the Kestrel upper to fake a circular orbit.
- Three.js only in the optical window (`optical.js` + `#sky`).
- VIEW vs mosaic TRACK: cameras are AUTO PAD TOWER SITE CHASE LIMB MAP.

## Commands

```
node test/run.js
```

## Modules

| File | Owner of |
|---|---|
| `physics.js` | Orbits, vehicles, RANGE autopilot |
| `optical.js` | 3D window |
| `audio.js` | Web Audio graph |
| `PAD-7.html` | Desk, phase, radio, composition |

Read `docs/adr/` before proposing a stack change.
